// Email sending — used for draft previews (scheduled job) and
// confirmation emails (Planning.confirmPlan). Build one template,
// adapt the other, per the Build Guide.

export async function sendDraftPreview(director, service, rankedList) {
  // TODO
}

export async function sendConfirmationEmail(director, service, confirmedSongs) {
  // TODO
}